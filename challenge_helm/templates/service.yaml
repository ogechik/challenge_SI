apiVersion: v1
kind: Service
metadata:
  name: {{ include "challenge_helm.fullname" . }}
  labels:
    {{- include "challenge_helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "challenge_helm.selectorLabels" . | nindent 4 }}
